<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Rebooter" parent="Theme.MaterialComponents">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/transparent</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Rebooter.Null" parent="Theme.Rebooter">
        <item name="android:windowAnimationStyle">@style/Theme.Rebooter.Null.WindowAnim</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:fitsSystemWindows">true</item>

        <item name="android:showOnLockScreen">true</item>
        <item name="android:showWhenLocked" tools:targetApi="o_mr1">true</item>
    </style>

    <style name="Theme.Rebooter.Null.WindowAnim">
        <item name="android:windowEnterAnimation">@anim/enter_anim</item>
        <item name="android:windowExitAnimation">@anim/exit_anim</item>

        <item name="android:activityOpenEnterAnimation">@anim/enter_anim</item>
        <item name="android:activityOpenExitAnimation">@anim/exit_anim</item>
        <item name="android:activityCloseEnterAnimation">@anim/enter_anim</item>
        <item name="android:activityCloseExitAnimation">@anim/exit_anim</item>
    </style>
</resources>